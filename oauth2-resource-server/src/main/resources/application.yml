server:
  port: 8081

spring:
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:18080/realms/oauth2
          jwk-set-uri: http://localhost:18080/realms/oauth2/protocol/openid-connect/certs

