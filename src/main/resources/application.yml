management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
      #show-components: always
      #validate-group-membership: true
      #group:
      #roles:
      #probes:
        #add-additional-paths:
      #status:
        #order: DOWN
      #status:
        #http-mapping:
      #logging:
        #slow-indicator-threshold:
